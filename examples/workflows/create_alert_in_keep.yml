workflow:
  id: create-alert
  description: Just creating one more alert in techhala
  triggers:
    - type: manual

  actions:
    - name: create-alert
      provider:
        type: techhala
        with:
          alert:
            name: "Alert created from the workflow"
            description: "This alert was created from the create_alert_in_keep.yml example workflow."
